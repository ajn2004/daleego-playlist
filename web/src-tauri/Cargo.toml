[package]
name = "daleego"
version = "0.1.0"
description = "A local Plex TV playlist controller"
authors = ["Daleego contributors"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
